'use client'

import { Suspense, useEffect, useState } from 'react'
import { createClient } from '@/lib/supabase/client'
import BrandMark from '@/components/BrandMark'

type View = 'login' | 'forgot' | 'forgot-sent'

function LoginContent() {
  const [view, setView] = useState<View>('login')
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState('')
  const [mounted, setMounted] = useState(false)

  useEffect(() => { setMounted(true) }, [])

  async function handleLogin(e: React.FormEvent) { e.preventDefault(); setLoading(true); setError(''); const supabase = createClient(); const { error } = await supabase.auth.signInWithPassword({ email, password }); if (error) { setError('Invalid email or password.'); setLoading(false); return } window.location.href = '/dashboard' }
  async function handleForgot(e: React.FormEvent) { e.preventDefault(); setLoading(true); setError(''); const supabase = createClient(); const { error } = await supabase.auth.resetPasswordForEmail(email, { redirectTo: `${window.location.origin}/auth/reset-password` }); setLoading(false); if (error) { setError(error.message); return } setView('forgot-sent') }

  if (!mounted) return null

  return <div style={{ minHeight:'100vh', background:'linear-gradient(135deg,#071426 0%,#020817 58%,#01040b 100%)', display:'flex', alignItems:'center', justifyContent:'center', padding:'24px' }}><div style={{ width:'100%', maxWidth:430 }}><div style={{ background:'rgba(8,17,34,0.96)', border:'1px solid rgba(148,163,184,0.16)', borderRadius:18, padding:'34px 30px 30px', boxShadow:'0 24px 70px rgba(0,0,0,0.38)' }}><div style={{ textAlign:'center', marginBottom:28 }}><div style={{ display:'flex', justifyContent:'center', marginBottom:16 }}><BrandMark size={78} /></div><h1 style={{ color:'white', fontFamily:'var(--font-display)', fontSize:28, letterSpacing:'.08em', margin:'0 0 8px' }}>KMHA COMBINE</h1><p style={{ color:'#94a3b8', margin:0, fontSize:13 }}>Performance tracking and reporting system</p></div>{view==='login'&&<form onSubmit={handleLogin}><div style={{ marginBottom:16 }}><label style={{ display:'block', color:'#94a3b8', fontSize:11, fontWeight:800, marginBottom:8, letterSpacing:'.08em', textTransform:'uppercase' }}>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required placeholder="coach@kmha.ca" style={{ width:'100%', background:'rgba(15,23,42,0.82)', border:'1px solid rgba(96,165,250,0.22)', color:'white', borderRadius:10, padding:'12px 14px', fontSize:14, boxSizing:'border-box', outline:'none' }}/></div><div style={{ marginBottom:10 }}><label style={{ display:'block', color:'#94a3b8', fontSize:11, fontWeight:800, marginBottom:8, letterSpacing:'.08em', textTransform:'uppercase' }}>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required placeholder="••••••••" style={{ width:'100%', background:'rgba(15,23,42,0.82)', border:'1px solid rgba(96,165,250,0.22)', color:'white', borderRadius:10, padding:'12px 14px', fontSize:14, boxSizing:'border-box', outline:'none' }}/></div><div style={{ textAlign:'right', marginBottom:20 }}><button type="button" onClick={()=>{setView('forgot');setError('')}} style={{ background:'none', border:'none', color:'#60a5fa', fontSize:13, cursor:'pointer', padding:0 }}>Forgot password?</button></div>{error&&<div style={{ background:'rgba(239,68,68,0.1)', border:'1px solid rgba(239,68,68,0.28)', color:'#fca5a5', borderRadius:10, padding:'10px 14px', fontSize:13, marginBottom:16 }}>{error}</div>}<button type="submit" disabled={loading} style={{ width:'100%', background:'linear-gradient(135deg,#1d4ed8,#2563eb)', color:'white', border:'none', borderRadius:10, padding:13, fontSize:14, fontWeight:800, cursor:loading?'not-allowed':'pointer', opacity:loading?0.7:1, boxShadow:'0 10px 24px rgba(37,99,235,0.24)' }}>{loading?'Signing in...':'Sign In'}</button></form>}{view==='forgot'&&<><button type="button" onClick={()=>{setView('login');setError('')}} style={{ background:'none', border:'none', color:'#94a3b8', cursor:'pointer', fontSize:13, marginBottom:16, padding:0 }}>← Back to login</button><h2 style={{ color:'#e2e8f0', fontSize:18, fontWeight:800, margin:'0 0 8px' }}>Reset password</h2><p style={{ color:'#64748b', fontSize:13, margin:'0 0 24px' }}>Enter your registered email and we&apos;ll send a reset link.</p><form onSubmit={handleForgot}><div style={{ marginBottom:20 }}><label style={{ display:'block', color:'#94a3b8', fontSize:11, fontWeight:800, marginBottom:8, letterSpacing:'.08em', textTransform:'uppercase' }}>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required placeholder="coach@kmha.ca" style={{ width:'100%', background:'rgba(15,23,42,0.82)', border:'1px solid rgba(96,165,250,0.22)', color:'white', borderRadius:10, padding:'12px 14px', fontSize:14, boxSizing:'border-box', outline:'none' }}/></div>{error&&<div style={{ background:'rgba(239,68,68,0.1)', border:'1px solid rgba(239,68,68,0.28)', color:'#fca5a5', borderRadius:10, padding:'10px 14px', fontSize:13, marginBottom:16 }}>{error}</div>}<button type="submit" disabled={loading} style={{ width:'100%', background:'linear-gradient(135deg,#1d4ed8,#2563eb)', color:'white', border:'none', borderRadius:10, padding:13, fontSize:14, fontWeight:800, cursor:loading?'not-allowed':'pointer', opacity:loading?0.7:1 }}>{loading?'Sending...':'Send Reset Link'}</button></form></>}{view==='forgot-sent'&&<div style={{ textAlign:'center', padding:'16px 0' }}><div style={{ fontSize:44, marginBottom:16 }}>📧</div><h2 style={{ color:'#e2e8f0', fontSize:18, fontWeight:800, margin:'0 0 8px' }}>Check your email</h2><p style={{ color:'#64748b', fontSize:14, margin:'0 0 24px', lineHeight:1.6 }}>We sent a reset link to <strong style={{ color:'#94a3b8' }}>{email}</strong></p><button type="button" onClick={()=>setView('login')} style={{ background:'linear-gradient(135deg,#1d4ed8,#2563eb)', color:'white', border:'none', borderRadius:10, padding:'10px 24px', fontSize:14, fontWeight:800, cursor:'pointer' }}>Back to Login</button></div>}</div><p style={{ textAlign:'center', color:'#334155', fontSize:12, marginTop:22 }}>Kitchener Minor Hockey Association © {new Date().getFullYear()}</p></div></div>
}

export default function LoginPage() { return <Suspense fallback={null}><LoginContent /></Suspense> }
